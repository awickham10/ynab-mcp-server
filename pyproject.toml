[project]
name = "ynab-mcp-server"
version = "0.1.0"
description = "Connect LLMs with your YNAB plans."
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "fastmcp==3.2.4",
    "httpx>=0.28.1",
    "pydantic>=2.13.4",
    "pydantic-settings>=2.14.1",
]
